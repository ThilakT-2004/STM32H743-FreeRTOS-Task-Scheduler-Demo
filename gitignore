# STM32CubeIDE build outputs
Debug/
Release/

# Compiled object files
*.o
*.d

# Linked binary outputs
*.elf
*.bin
*.hex
*.map
*.lst

# CubeIDE workspace metadata (machine-specific, don't commit)
.metadata/
.settings/
*.launch
RemoteSystemsTempFiles/

# CubeIDE project files (regenerated by CubeMX)
# Keep .cproject and .project if teammates use same IDE
# Uncomment below to exclude them:
# *.cproject
# *.project

# macOS junk
.DS_Store

# Windows junk
Thumbs.db
desktop.ini

# Renode scripts (local only)
*.resc

# ─── Keep these ───────────────────────────────────────
# CubeMX config — needed to regenerate code
!*.ioc

# Source and headers
!Core/Src/*.c
!Core/Inc/*.h

# FreeRTOS kernel and HAL drivers
!Middlewares/
!Drivers/
